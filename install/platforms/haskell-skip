#!/usr/bin/env bash

# NOTE: on a Mac, one must setup the /Applications/GHC.app (download, extract and rename)
# http://ghcformacosx.github.io/

# TODO: Find out how to install Haskell on a Linux box - Cabal is not fun...


# https://github.com/gibiansky/IHaskell

# On a Mac, zeromq would be auto-installed via brew?
if onApt; then
  sudo apt-get install libzmq3-dev
fi
